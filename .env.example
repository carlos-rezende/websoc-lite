# Copie para .env e ajuste. Docker Compose carrega .env automaticamente.
# Token para os botões "Parar pesquisa" / "Iniciar pesquisa" no painel (header X-Control-Token).
PANEL_CONTROL_TOKEN=altere_esta_senha
